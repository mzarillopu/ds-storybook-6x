import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';

<Meta title="Components/Link" component="Link" />

export const Template = (args) => html`
    <a href="javascript:void(0);">${ args.text }</a>
`;

export const VisitedTemplate = (args) => html`
    <a href="javascript:void(0);" class="emc-link-visited">${ args.text }</a>
`;

export const AlternativeVisitedTemplate = (args) => html`
    <div class="emc-highlight-visited">
        <a href="javascript:void(0);" class="emc-link-visited">${ args.text }</a>
    </div>
`;

export const FocusTemplate = (args) => html`
    <a href="javascript:void(0);" class="emc-link-focus">${ args.text }</a>
`;

export const ActiveTemplate = (args) => html`
    <a href="javascript:void(0);" class="emc-link-active">${ args.text }</a>
`;

export const HoverTemplate = (args) => html`
    <a href="javascript:void(0);" class="emc-link-hover">${ args.text }</a>
`;

export const DisabledTemplate = (args) => html`
    <a href="javascript:void(0);" class="emc-link-disabled" disabled>${ args.text }</a>
`;

export const ExternalLinkTemplate = (args) => html`
    <a href="javascript:void(0);" target="_blank" rel="noopener noreferrer">${ args.text }</a>
`;

# Link

A link (or "hyperlink") is a word or phrase that the user can click on to navigate to a new web page/document or
navigate to a different section within the same page/document.

## Implementation Notes

<i>TODO: The "external" icon needs aligned and does not scale with the surrounding text as it should.</i>

<Canvas>
    <Story name="Simple Link" args={{
        text: "Link Text"
    }}>
      {Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="External Link" args={{
        text: "Link Text"
    }}>
      {ExternalLinkTemplate.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Visited Link" args={{
        text: "Link Text"
    }}>
      {VisitedTemplate.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Visited Link Override" args={{
        text: "Link Text"
    }}>
      {AlternativeVisitedTemplate.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Focused" args={{
        text: "Link Text"
    }}>
      {FocusTemplate.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Active" args={{
        text: "Link Text"
    }}>
      {ActiveTemplate.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Hover" args={{
        text: "Link Text"
    }}>
      {HoverTemplate.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Disabled" args={{
        text: "Link Text"
    }}>
      {DisabledTemplate.bind({})}
    </Story>
</Canvas>
