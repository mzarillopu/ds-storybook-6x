import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';

<Meta title="Components/Pill" component="Pill" />

export const PillTemplate = (args) => html`
    <a href="javascript:void(0);" class="emc-pill">${ args.content }</a>
`;

export const PillBagTemplate = (args) => html`
    <ul class="emc-pill-bag">
        <li><a href="javascript:void(0);">${ args.content }</a></li>
        <li><a href="javascript:void(0);">${ args.content }</a></li>
        <li><a href="javascript:void(0);">${ args.content }</a></li>
    </ul>
`;

# Pill

A Pill is a tag that often looks like a button with rounded corners.  It can be used for identify related tags, 
areas of study, subjects, departments, or any other categorization.

<Canvas>
    <Story name="Simple" args={{
        content: 'Sample Pill',
    }}>
      {PillTemplate.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Pill Bag" args={{
        content: 'Sample Pill'
    }}>
      {PillBagTemplate.bind({})}
    </Story>
</Canvas>
