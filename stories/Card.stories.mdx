import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';

<Meta title="Components/Card" component="Card" />

export const Template = (args) => html`
    <div class="emc-card">
        <h2 class="emc-card__heading">${ args.heading }</h2>
        <div class="emc-card__content">
            ${ args.bodyContent }
        </div>
        <div class="emc-card__footer">
            ${ args.footerContent }
        </div>
    </div>
`;

# Cards

<Canvas>
    <Story name="Basic" args={{
        heading: 'Heading - With Enough Content To Test Text Wrapping',
        bodyContent: 'This is the content that will go in the body of the card.  We will want enough content to allow us to test wrapping.',
        footerContent: 'Footer Content'
    }}>
      {Template.bind({})}
    </Story>
</Canvas>
