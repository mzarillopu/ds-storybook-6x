import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';

<Meta title="Components/Heading" component="Heading" />

export const H1Template = (args) => html`
    <h1>Heading Content (H1)</h1>
`;

export const H2Template = (args) => html`
    <h2>Heading Content (H2)</h2>
`;

export const H3Template = (args) => html`
    <h3>Heading Content (H3)</h3>
`;

export const H4Template = (args) => html`
    <h4>Heading Content (H4)</h4>
`;

export const H5Template = (args) => html`
    <h5>Heading Content (H5)</h5>
`;

export const H6Template = (args) => html`
    <h6>Heading Content (H6)</h6>
`;

export const PageTitleTemplate = (args) => html`
    <h1 class="puds-page-title">${ args.content }</h1>
`;

export const BlockHeadingTemplate = (args) => html`
    <h1 class="puds-block-heading">${ args.content }</h1>
`;

# Heading

## Implementation Notes

Include some notes

<Canvas>
    <Story name="Heading 1" args={{
        content: "Heading Content"
    }}>
      {H1Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Heading 2" args={{
        content: "Heading Content"
    }}>
      {H2Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Heading 3" args={{
        content: "Heading Content"
    }}>
      {H3Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Heading 4" args={{
        content: "Heading Content"
    }}>
      {H4Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Heading 5" args={{
        content: "Heading Content"
    }}>
      {H5Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Heading 6" args={{
        content: "Heading Content"
    }}>
      {H6Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Page Title" args={{
        content: "Page Title"
    }}>
      {PageTitleTemplate.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Block Heading" args={{
        content: "Block Heading"
    }}>
      {BlockHeadingTemplate.bind({})}
    </Story>
</Canvas>
