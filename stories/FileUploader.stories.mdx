import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';

<Meta title="Components/File Uploader" component="FileUploader" />

export const DefaultFileUploaderTemplate = (args) => html`
  <strong class="emc-file-uploader--label">Please select a file</strong>
  <p class="emc-file-uploader--label-description">only .jpg and .png files. 500kb max file size.</p>
  <label class="emc-btn">Add a New File <input type="file" style="display: none;" [multiple]="multiple" #fileInput></label>
`;

# File Uploader

File Uploader allows the user to transfer a file or submit content of their own.

## Default
The default form of the file uploader

<Canvas>
    <Story name="Default" args={{
        }}>
        {DefaultFileUploaderTemplate.bind({})}
    </Story>
</Canvas>
