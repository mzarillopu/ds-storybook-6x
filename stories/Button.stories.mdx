import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';

<Meta title="Components/Buttons" component="Button" />

export const Template = (args) => html`
    <button class="emc-btn">${ args.label }</button>
    <button class="emc-btn emc-btn--hover">${ args.label }</button>
    <button class="emc-btn emc-btn--active">${ args.label }</button>
    <button class="emc-btn emc-btn--focus emc-focus">${ args.label }</button>
    <button class="emc-btn" disabled>${ args.label }</button>
    <button class="emc-btn emc-btn--unstyled">${ args.label }</button>
`;

export const DefaultTemplate = (args) => html`
    <button class="emc-btn">${ args.label }</button>
`;

export const HoverTemplate = (args) => html`<button class="emc-btn emc-btn--hover">${ args.label }</button>`;

export const ActiveTemplate = (args) => html`<button class="emc-btn emc-btn--active">${ args.label }</button>`;

export const FocusTemplate = (args) => html`
    <button class="emc-btn emc-btn--focus emc-focus">${ args.label }</button>
`;

export const DisabledTemplate = (args) => html`<button class="emc-btn" disabled>${ args.label }</button>`;

export const UnstyledTemplate = (args) => html`<button class="emc-btn emc-btn--unstyled">${ args.label }</button>`;

# Buttons

## TODO

* Disabled mouse pointer should change

<Canvas>
    <Story name="Button States" args={{
        label: "Label",
    }}>
      {Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Default" args={{
        label: "Label",
    }}>
      {DefaultTemplate.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Hover" args={{
        label: "Label",
    }}>
      {HoverTemplate.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Active" args={{
        label: "Label",
    }}>
      {ActiveTemplate.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Focus" args={{
        label: "Label",
    }}>
      {FocusTemplate.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Disabled" args={{
        label: "Label",
    }}>
      {DisabledTemplate.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Unstyled" args={{
        label: "Label",
    }}>
      {UnstyledTemplate.bind({})}
    </Story>
</Canvas>
